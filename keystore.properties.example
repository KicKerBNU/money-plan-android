# Copy to keystore.properties (gitignored) after creating the upload keystore.
# See scripts/create-upload-keystore.sh or README → Release signing.

storeFile=release/money-plan-upload.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=money-plan-upload
keyPassword=YOUR_KEY_PASSWORD
